/*

class Programm
{
    static void Main()
    {
        HabitManager manager = new HabitManager();
        JsonHabitSerializer serializer = new JsonHabitSerializer();
        manager.AddHabit("Привычка 1", "Без описания", Habit.Type.negative);
        manager.AddHabit("Привычка 2", "Описание", Habit.Type.positive);
        manager.AddHabit("Привычка 1", "Без описания", Habit.Type.negative);
        manager.AddHabit("Привычка 2", "Описание", Habit.Type.positive);
        manager.AddHabit("Привычка 1", "Без описания", Habit.Type.negative);
        manager.AddHabit("Привычка 2", "Описание", Habit.Type.positive);
        
        for (int i = 0; i < manager.habitList.Count; i++)
        {
            serializer.Serialize(manager.habitList[i]);
        }

    }
}
*/